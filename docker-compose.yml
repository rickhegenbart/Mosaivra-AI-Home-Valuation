services:
  backend:
    build:
      context: ./backend
    container_name: mosaivra_ai_home_valuation_backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:10000"
    volumes:
      - ./backend:/app
